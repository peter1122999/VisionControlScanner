import Foundation

enum SiriScene {
    static let definition = SceneDefinition(
        identifier: "siri",
        displayName: "Siri",
        matchKeywords: ["enable ask siri", "hey siri", "siri can help you"],
        layout: .checkboxList
    )
}
